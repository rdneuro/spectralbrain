:orphan:

Example Gallery
===============

Short, self-contained recipes — each one runs in seconds on a synthetic surface
and produces the figure you see in its thumbnail. They are deliberately light
(small mesh, few eigenpairs) so the whole gallery rebuilds quickly; swap the
synthetic shape for ``sb.load_freesurfer_surface(...)`` to run them on a real
brain.
