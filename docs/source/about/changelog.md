# Changelog

The full, authoritative changelog is maintained in
[`CHANGELOG.md`](https://github.com/rdneuro/spectralbrain/blob/main/CHANGELOG.md)
at the repository root and follows
[Keep a Changelog](https://keepachangelog.com/) with semantic versioning.

```{note}
To surface the changelog inline here instead of linking out, copy
`CHANGELOG.md` into `docs/source/about/` during the build and replace this page
with an `{include}` directive:

    ```{include} _changelog_copy.md
    ```
```
